diff --git a/node_modules/@neteasecloudmusicapienhanced/api/module/song_url_match.js b/node_modules/@neteasecloudmusicapienhanced/api/module/song_url_match.js
index d2adbb0..7daf241 100644
--- a/node_modules/@neteasecloudmusicapienhanced/api/module/song_url_match.js
+++ b/node_modules/@neteasecloudmusicapienhanced/api/module/song_url_match.js
@@ -21,7 +21,7 @@ module.exports = async (query, request) => {
       status: 200,
       body: {
         code: 200,
-        data: result.data.url,
+        data: result.data ? result.data.url : null,
         proxyUrl: result.proxyUrl || '',
       },
     }
diff --git a/node_modules/@neteasecloudmusicapienhanced/api/module/song_url_v1.js b/node_modules/@neteasecloudmusicapienhanced/api/module/song_url_v1.js
index 03b3f49..b74d47f 100644
--- a/node_modules/@neteasecloudmusicapienhanced/api/module/song_url_v1.js
+++ b/node_modules/@neteasecloudmusicapienhanced/api/module/song_url_v1.js
@@ -17,34 +17,74 @@ module.exports = async (query, request) => {
   }
   if (query.unblock === 'true') {
     try {
-      const result = await matchID(query.id, query.source)
-      logger.info('Starting unblock(uses modules unblock):', query.id, result)
-      const useProxy = process.env.ENABLE_PROXY || 'false'
-      let proxyUrl = ''
-      if (result.data.url && result.data.url.includes('kuwo')) {
-        proxyUrl =
-          useProxy === 'true' && process.env.PROXY_URL
-            ? process.env.PROXY_URL + result.data.url
-            : result.data.url
+      let unblockUrl = null
+      let unblockSource = null
+
+      // 客户端提供了歌曲元数据时，直接用 UNM 全量音源匹配（绕过版权下架导致的 name:null）
+      if (query.unblock_name) {
+        try {
+          const unmMatch = require('@unblockneteasemusic/server')
+          const songData = {
+            id: Number(query.id),
+            name: query.unblock_name,
+            artists: (query.unblock_artist || query.unblock_name).split('/').map((name, i) => ({
+              id: i, name: name.trim(),
+            })),
+            album: { id: 0, name: query.unblock_album || '' },
+            duration: Number(query.unblock_duration) || 0,
+          }
+          const allSources = process.env.UNBLOCK_SOURCES
+            ? process.env.UNBLOCK_SOURCES.split(',')
+            : ['bodian', 'kuwo', 'kugou', 'qq', 'migu', 'bilibili']
+          const resp = await unmMatch(query.id, allSources, songData)
+          if (resp && resp.url) {
+            unblockUrl = resp.url
+            unblockSource = resp.source || 'unm'
+          }
+        } catch (e) {
+          logger.info('UNM match with metadata failed, falling back:', e.message)
+        }
+      }
+
+      // 元数据匹配未成功则回退 matchID 遍历外部模块
+      if (!unblockUrl) {
+        const result = await matchID(query.id, query.source)
+        logger.info('Starting unblock(uses modules unblock):', query.id, result)
+        if (result.data && result.data.url) {
+          unblockUrl = result.data.url
+          unblockSource = result.data.source
+        }
       }
-      return {
-        status: 200,
-        body: {
-          code: 200,
-          msg: 'Warning: Customizing unblock sources is not supported on this endpoint. Please use `/song/url/match` instead.',
-          data: [
-            {
-              id: Number(query.id),
-              url: result.data.url,
-              type: 'flac',
-              level: query.level,
-              freeTrialInfo: 'null',
-              fee: 0,
-              proxyUrl: proxyUrl || '',
-            },
-          ],
-        },
-        cookie: [],
+
+      if (unblockUrl) {
+        const useProxy = process.env.ENABLE_PROXY || 'false'
+        let proxyUrl = ''
+        if (unblockUrl.includes('kuwo')) {
+          proxyUrl =
+            useProxy === 'true' && process.env.PROXY_URL
+              ? process.env.PROXY_URL + unblockUrl
+              : unblockUrl
+        }
+        logger.info('Unblock matched:', query.id, unblockSource, unblockUrl)
+        return {
+          status: 200,
+          body: {
+            code: 200,
+            msg: `Unblock matched via ${unblockSource}`,
+            data: [
+              {
+                id: Number(query.id),
+                url: unblockUrl,
+                type: 'flac',
+                level: query.level,
+                freeTrialInfo: 'null',
+                fee: 0,
+                proxyUrl: proxyUrl || '',
+              },
+            ],
+          },
+          cookie: [],
+        }
       }
     } catch (e) {
       console.error('Error in unblocking music:', e)
diff --git a/node_modules/@neteasecloudmusicapienhanced/api/server.js b/node_modules/@neteasecloudmusicapienhanced/api/server.js
index a28f081..5eaf51d 100644
--- a/node_modules/@neteasecloudmusicapienhanced/api/server.js
+++ b/node_modules/@neteasecloudmusicapienhanced/api/server.js
@@ -339,9 +339,11 @@ async function constructServer(moduleDefs) {
             } = require('@neteasecloudmusicapienhanced/unblockmusic-utils')
             logger.info('Starting unblock(uses general unblock):', req.query.id)
             const result = await matchID(req.query.id)
-            song.url = result.data.url
-            song.freeTrialInfo = null
-            logger.info('Unblock success! url:', song.url)
+            if (result.data && result.data.url) {
+              song.url = result.data.url
+              song.freeTrialInfo = null
+              logger.info('Unblock success! url:', song.url)
+            }
           }
           if (song.url && song.url.includes('kuwo')) {
             const proxy = process.env.PROXY_URL
